import { style } from '@vanilla-extract/css';

export const paramsCls = style({
    display: 'flex',
    flexDirection: 'column',
    gap: '10px',
});

export const paramIconCls = style({
    cursor: 'pointer',
    marginLeft: '10px',
});
