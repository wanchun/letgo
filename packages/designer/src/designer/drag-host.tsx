import { defineComponent } from 'vue';
import './drag-host.less';

export const DragHostView = defineComponent({
    name: 'DragHostView',
    setup() {
        return () => {
            return null;
        };
    },
});
