import { style } from '@vanilla-extract/css';

export const labelWrapCls = style({
    display: 'flex',
    alignItems: 'center',
});

export const labelTextCls = style({
    flex: '0 0 96px',
    color: '#555',
    marginRight: '8px',
});
