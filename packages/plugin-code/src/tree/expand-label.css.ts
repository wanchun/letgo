import { style } from '@vanilla-extract/css';

export const expandIconCls = style({
    cursor: 'pointer',
    fontSize: '16px',
    display: 'inline-block',
    color: '#bfbfbf',
    lineHeight: 0,
});
