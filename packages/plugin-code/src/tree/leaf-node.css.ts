import { style } from '@vanilla-extract/css';

export const leafCls = style({
    display: 'flex',
    flexWrap: 'wrap',
});

export const leafValueCls = style({
    paddingLeft: '8px',
    whiteSpace: 'normal',
    wordBreak: 'break-all',
});
