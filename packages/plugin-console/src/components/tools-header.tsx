import { defineComponent } from 'vue';

export default defineComponent({
    setup() {
        return () => {
            return (
                <div class="letgo-devtools-headers">
                    错误
                </div>
            );
        };
    },
});
